export const TipAsHTML = (tipObj) => {
  return `
    <section class="tip">
        <div>${tipObj}</div>
    </section>
    <section class="tip">
        <div>${tipObj}</div>
    </section>
    <section class="tip">
        <div>${tipObj}</div>
    </section>
    `
}
